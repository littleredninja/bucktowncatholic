<h2>Upcoming Events</h2>

<% @events.each do |event| %>
    <div>
      <p><%= event.title %><p>
      <small><%= event.description %></small>
      <small><%= stringify_date(event) %></small>
    </div>
<% end %>
